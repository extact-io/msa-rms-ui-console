config_ordinal: 200
#----------------------------------
# -console.jar Test Configurations
#----------------------------------
web-api/mp-rest/url: http://localhost:7001

mp:
  jwt:
    verify.publickey.location: /jwt.pub.key

rms:
  jwt:
    privatekey.path: /jwt.key
    claim.issuer: RentalReservationSystem
  debug:
    configdump:
      filter-enable: true
      filters:
        #- filter: security
        - filter: server
